---
"effect-view-server": minor
---

Expose the canonical wire-safe BigDecimal admission, identity, allocation-safe comparison, and reusable opaque comparison-metadata contracts through the focused `effect-view-server/value-semantics` public subpath.
