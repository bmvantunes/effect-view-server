---
name: vite
description: Use for Vite and Vite+ configuration, plugins, builds, library packaging, SSR, and migrations in this repository.
---

# Vite and Vite+

Read `node_modules/vite-plus/AGENTS.md` completely before changing Vite or Vite+
configuration. Read only the relevant pages under `node_modules/vite-plus/docs`.

Use installed-version evidence:

- Run `vp toolchain vite` when the active Vite/Rolldown version matters.
- Inspect installed declarations or source for exact APIs not covered by the docs.
- Check `vite.config.ts`, package scripts, and similar project code before adding a new pattern.

Use `vp` commands as required by the repository. Repository instructions override
general package guidance.
