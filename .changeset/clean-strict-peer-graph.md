---
"effect-view-server": patch
---

Publish one aligned Effect RC dependency graph and keep the private TypeScript compiler API alias out of the public package so strict external consumers resolve the declared Effect and TypeScript peers without overrides.
