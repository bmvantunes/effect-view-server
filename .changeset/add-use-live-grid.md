---
"effect-view-server": minor
---

Add `useLiveGrid` on `createViewServerReact` for typed live viewport datasources (`init` / full-state `onChange` / window-only `onScroll` / `destroy`, absolute-index sink, raw/grouped windows) alongside `useLiveQuery`.
