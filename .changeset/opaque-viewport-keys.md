---
"effect-view-server": patch
---

Expose an opaque source-owned semantic key for Live Query Viewport inputs so adapters can compare freshly allocated exact queries without duplicating schema or route semantics.
